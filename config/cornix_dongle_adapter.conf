CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=80

# TEMPORARY (recovery build):
# Wipe the ENTIRE settings area (keymap/Studio data, bonds, everything) on every boot.
# Flash once, power on once (10s), then flash normal firmware.
CONFIG_ZMK_SETTINGS_RESET_ON_START=y
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=y
